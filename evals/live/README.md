# Live Agent regression — 2026-07-22

These six outputs were generated by `gpt-5.6-sol` through `codex exec` with the installed `valorant-cn-performance` Skill explicitly requested. Each run used an isolated temporary working directory and was instructed to read only the Skill and its references.

Validation command:

```bash
python3 tests/validate.py --outputs evals/live
```

Scope and limits:

- These are real model outputs, not hand-authored fixtures.
- They test routing, safety, current-version facts, output structure, and rollback language.
- They do not prove Windows PowerShell runtime compatibility, real FPS gains, network improvements, or anti-cheat compatibility on a user's machine.
- No Windows PC, VALORANT client, ACE, or Vanguard instance was controlled during these runs.
